# TextileOps — מערכת ניהול מפעל בגדים

מערכת ניהול למפעל טקסטיל: נוכחות עובדים, משימות ייצור לפי ברקוד, דוחות, עלויות, מוצרים והתראות טלגרם. הממשק בעברית (RTL) עם תמיכה בערבית. המנהל (המשתמש שמדבר איתך) כותב בעברית.

## ארכיטקטורה — קובץ אחד!

- **כל האפליקציה היא קובץ אחד: `index.html`** (~5,000 שורות) — HTML + CSS + JS באותו קובץ. אין build, אין framework.
- **פריסה:** push ל-`main` ← GitHub Pages מפרסם אוטומטית תוך 2-3 דקות ל-https://amtextile2222-beep.github.io/textileops/
- **בסיס נתונים:** Firebase Firestore (פרויקט `textileops-aef4a`), סנכרון בזמן אמת בין מכשירים דרך `onSnapshot`. יש גם עותק מקומי ב-localStorage (`txops_v3`) לעבודה לא מקוונת.
- **אימות Firebase (שלב 2 — 03/07/2026):** אין יותר התחברות אנונימית. `doLogin()` קורא ל-Cloud Function `login` (מאמתת סיסמה/פנים/מכשיר/IP בצד שרת) שמחזירה **Custom Token עם `role` claim**; הלקוח נכנס עם `signInWithCustomToken`. ה-Rules דורשים `role` בטוקן — בלי כניסה דרך הפונקציה שום דבר לא נטען. ה-listeners (`startDataListeners`) מתחילים רק אחרי כניסה.

## חובה לפני כל commit

1. בדיקת תחביר לשני בלוקי הסקריפט (הקובץ גדול מכדי לסמוך על העין):
   ```bash
   # לחלץ את התוכן של <script>...</script> ושל <script type="module">...</script>
   # ולהריץ עליהם node --check
   ```
2. אסור להכניס לקוד או ל-repo סודות: טוקן הטלגרם נשמר רק ב-Firestore (`appSettings/telegramSettings`) וב-localStorage — לעולם לא בקוד.
3. שמור על סגנון הקוד הקיים: JS מצומצם (minified-style), הערות בעברית, פונקציות גלובליות (אין מודולים בבלוק הראשי).

## מבנה הנתונים (אובייקט S גלובלי + אוספי Firestore)

| אוסף Firestore | מה יש בו |
|---|---|
| `workers` | עובדים: id, name, dept, role, user, sessions, requireFactoryIP, faceRegistered/deviceRegistered (מירורים לתצוגה בלבד) |
| `credentials` | **סודות כניסה — אין גישת לקוח בכלל**: pass (SHA-256 `$h:`), faceDescriptors, deviceId. נכתב רק דרך Cloud Functions (`login`, `updateCredentials`) |
| `adminSettings` | costs (שכר/עלויות) — קריאה/כתיבה למנהל בלבד |
| `activeTasks` / `histTasks` | משימות ייצור (פעילות / היסטוריה). שדה `bc` = ברקוד 13 ספרות |
| `attHistory` | נוכחות יומית, מפתח מסמך: `workerId_YYYY-MM-DD` |
| `products` | מוצרים: תמונות, מסמכים, כמויות, שלבי ייצור (stageLog/stageSkipped — תמיד מערכים!) |
| `appSettings` | customerNames, customColors, telegramSettings, dailyReport (נעילה), punchOutAlert (נעילה). costs עבר ל-adminSettings |

## תפקידים (role)

`manager` (מנהל — רואה הכל), `supervisor` (אחראי), `worker` / `helper` (עובד — רואה רק את המשימות של עצמו בעמודי משימות/סריקה; אין לו כפתורי עריכה).

## מבנה ברקוד (13 ספרות)

`XX` לקוח (2) + `XXXXX` מוצר (5) + `X` מידה (1) + `XXX` כמות (3) + `XX` קוד צבע (2)

### כללי ספקים
- **רונן חן — קוד לקוח 01:** מספר בתעודה 5 ספרות ישיר. שורות שמתחילות ב-9 (כמו `914613`) = דגם מקורי, מתעלמים. ה-PDF דיגיטלי — מפוענח עם pdf.js.
- **רעומה זקש — קוד לקוח 02:** מספר בתעודה 8 ספרות ← מסירים 2 ראשונות ← לוקחים 4 ← מוסיפים 0 בהתחלה (למשל `09715516` ← `07155`). התעודות סרוקות — OCR עם Tesseract.js.

## כללים עסקיים חשובים — אל תשבור

1. **משימות מקבילות:** עובד יכול לעבוד על כמה ברקודים במקביל (יש דיאלוג confirm). שני עובדים יכולים לחלוק ברקוד באותו שלב.
2. **ספירת יחידות בלי כפילויות:** `sumUnitsDedup()` — יחידות נספרות פעם אחת לברקוד+שלב+יום. אותו ברקוד בשלבים שונים (תפירה←גיהוץ←אריזה) נספר בכל שלב בנפרד — זה מכוון.
3. **דוח נוכחות יומי ב-15:15 לטלגרם** (הודעה + CSV) — עם נעילה אטומית ב-`runTransaction` (`appSettings/dailyReport`) כדי שרק מכשיר אחד ישלח. אם אף אחד לא נכח — נשלחת שורה אחת "אין נוכחות היום".
4. **התראת 15:45** — רשימת עובדים שלא החתימו יציאה (נעילה: `appSettings/punchOutAlert`).
5. **איפוס יומי בחצות** (`runDailyReset`) — שומר נוכחות ל-attHistory, סוגר משימות פתוחות גם ב-Firebase. ה-onSnapshot של activeTasks סוגר משימות זומבי מימים קודמים (רק כש-`!snap.metadata.fromCache`).
6. **דוחות ולוח בקרה גלויים לכל העובדים** — החלטת הלקוח, לא לסנן בלי לשאול.
7. שעות נטו = זמן עבודה פחות הפסקה (ברירת מחדל 13:00–13:45, `breakActive`).

## אבטחה

- כניסת חירום: 5 לחיצות על הלוגו ✂️ במסך הכניסה. הקוד מאומת ב-Cloud Function (`app.emergencyhash` ב-functions:config); fallback מקומי כשאין רשת.
- עובדים עם `requireFactoryIP` נבדקים מול IP המפעל (188.225.231.120) **בצד שרת** בפונקציית login + ניטור כל 5 דקות (wifiMonitor).
- זיהוי פנים: face-api.js בלקוח מפיק descriptor; **ההשוואה בשרת** (euclidean < 0.55 ב-login CF). דסקריפטורים שמורים ב-`credentials` בלבד.
- נעילת מכשיר: UUID ב-localStorage (`txops_device_uuid`), נבדק ונרשם בצד שרת ב-login.
- כל אירוע חריג (קוד חירום שגוי, זיהוי פנים נכשל, כניסה מחוץ ל-WiFi, מכשיר לא מורשה) שולח התראת טלגרם **מהשרת**.
- נעילת ניסיונות בשרת: 5 כשלונות ← דקה חסימה (בנוסף לנעילה המקומית בלקוח).

## סגנון תקשורת עם הלקוח

תשובות קצרות וענייניות בעברית. לא להציע פתרונות בלי לבדוק קודם את הקוד. לסכם מה נעשה בסוף. commit messages באנגלית.
